import { PRICE_CFG } from "./config.js";

export class Store {
  buyItem(itemName) {
    if (this.isGameOver) return;

    switch (itemName) {
      case 'autoHeal':

        break;
      case 'unlockHero':

        break;
      default:
        break;
    }
  }
}